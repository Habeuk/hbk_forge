/**
 * @file
 * HBK cforge behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.hbkCforge = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
